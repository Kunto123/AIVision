# VisionInspect - PC Edge (CPU) requirements
# Python 3.10+ required
# Gunakan: pip install -r requirements_edge.txt
#
# PC Edge = INFERENCE (jalur live OpenVINO + auth + API internal).
# Training (termasuk YOLO) dilakukan di PC Dev → requirements_dev.txt.
# Kombinasi constraint di sini diverifikasi (2026-08): anomalib 2.5.0,
# numpy 2.4.6, torch 2.13.0+cpu, openvino 2026.2.1, PySide6 6.6.3.1.
#
# ─────────────────────────────────────────────────────────────────────
# CATATAN PENTING — DEPENDENSI DI LUAR PyPI
# ─────────────────────────────────────────────────────────────────────
# Satu paket TIDAK tersedia di PyPI dan diambil langsung dari GitHub:
#
#   fxplc — protokol port pemrograman Mitsubishi FX (FX Computer Link),
#           jalur yang sama dengan GX Works2. Dipakai untuk komunikasi PLC
#           di visioninspect/plc/fx_link.py.
#
#   Repo   : https://github.com/KrystianD/fxplc
#   Commit : ed1e5aab2a1ff9e5418a301649322cbeceaabae8
#   Lisensi: MIT
#
# TIDAK PERLU GIT. Baris di bawah memakai URL arsip .zip GitHub, jadi pip
# cukup mengunduh lewat HTTPS biasa. Ini disengaja: PC edge Windows sering
# tidak punya executable `git`, dan bentuk `git+https://` akan gagal di sana
# walau internetnya ada.
#
# Yang dibutuhkan PC edge hanya AKSES INTERNET ke github.com.
#
# Kalau suatu saat perlu meng-clone manual (mis. untuk membaca/memodifikasi
# sumbernya):
#     git clone https://github.com/KrystianD/fxplc.git
#     cd fxplc && git checkout ed1e5aab2a1ff9e5418a301649322cbeceaabae8
#     pip install .
#
# Kalau PC edge TANPA internet, ada wheel siap pakai di repo ini:
#     pip install --no-index --find-links vendor fxplc
# (vendor/fxplc-0.4.0-py3-none-any.whl)
# ─────────────────────────────────────────────────────────────────────

--extra-index-url https://download.pytorch.org/whl/cpu

# ===== GUI =====
PySide6>=6.6.0,<6.7

# ===== Computer Vision =====
opencv-python-headless>=4.9.0,<5.0
numpy>=1.24,<3.0
timm>=1.0,<1.1
huggingface_hub>=0.24,<2.0

# ===== Anomaly Detection =====
anomalib[core]>=1.1,<3.0
scikit-learn>=1.3,<2.0
requests>=2.28,<3.0
torch>=2.1,<3.0
torchvision>=0.16,<1.0
python-dotenv
pandas>=2.0,<4.0
matplotlib>=3.7,<4.0

# ===== OpenVINO =====
openvino>=2024.0
onnx>=1.16.0
onnxscript

# ===== Serial / PLC =====
pyserial>=3.5,<4.0
# fxplc — lihat CATATAN PENTING di bagian atas file ini.
# Dipin ke commit supaya build bisa diulang; tanpa pin, HEAD bisa berubah
# kapan saja dan itu tidak pantas untuk sistem produksi.
fxplc @ https://github.com/KrystianD/fxplc/archive/ed1e5aab2a1ff9e5418a301649322cbeceaabae8.zip
#
# pymodbus DIHAPUS: FX3U menuntut adaptor -MB khusus untuk MODBUS, dan unit
# di lini ini tidak memilikinya — D8400/D8401 tetap nol berapa kali pun
# di-power-cycle. Transport diganti FX Computer Link.

# ===== Keamanan / API internal =====
# cryptography: enkripsi Fernet secret_store (password PG) — fallback XOR
#               stdlib hanya dipakai kalau paket ini tidak ada
cryptography>=42.0
# werkzeug: diimport langsung oleh api/flask_app.py (make_server)
werkzeug>=3.0,<4.0

# ===== Optional =====
flask>=3.0,<4.0
psycopg2-binary>=2.9,<3.0
psutil>=5.9,<8.0
pytest>=8.0,<10.0
