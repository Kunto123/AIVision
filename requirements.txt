# VisionInspect - Industrial Visual Inspection System
# Python 3.10+ required
# NOTE: PC Dev (GPU) → pakai requirements_dev.txt
#       PC Edge (CPU) → pakai file ini atau requirements_edge.txt
#
# Diverifikasi (2026-08): kombinasi teruji E2E di WSL —
#   anomalib 2.5.0, numpy 2.4.6, torch 2.13.0+cpu, ultralytics 8.4.117,
#   openvino 2026.2.1, PySide6 6.6.3.1. Constraint di bawah MENGIZINKAN
#   kombinasi itu (pip resolve memilih versi tertinggi yang kompatibel).

--extra-index-url https://download.pytorch.org/whl/cpu

# ===== GUI =====
PySide6>=6.6.0,<6.7

# ===== Computer Vision =====
opencv-python-headless>=4.9.0,<5.0
numpy>=1.24,<3.0
timm>=1.0,<1.1
huggingface_hub>=0.24,<2.0

# ===== Anomaly Detection =====
anomalib[core]>=1.1,<3.0
scikit-learn>=1.3,<2.0
requests>=2.28,<3.0
torch>=2.1,<3.0
torchvision>=0.16,<1.0
python-dotenv
pandas>=2.0,<4.0
matplotlib>=3.7,<4.0

# ===== OpenVINO =====
openvino>=2024.0
onnx>=1.16.0
onnxscript

# ===== Serial / PLC =====
pyserial>=3.5,<4.0
pymodbus>=3.6,<4.0

# ===== Keamanan / API internal =====
# cryptography: enkripsi Fernet secret_store (password PG) — fallback XOR
#               stdlib hanya dipakai kalau paket ini tidak ada
cryptography>=42.0
# werkzeug: diimport langsung oleh api/flask_app.py (make_server)
werkzeug>=3.0,<4.0

# ===== Optional =====
flask>=3.0,<4.0
psycopg2-binary>=2.9,<3.0
psutil>=5.9,<8.0
pytest>=8.0,<10.0
