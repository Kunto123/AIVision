# VisionInspect - PC Dev (GPU) requirements
# Python 3.10+ required
# Gunakan: pip install -r requirements_dev.txt
#
# PC Dev = TRAINING (anomalib, YOLO, INT8 export) + testing + PG.
# Diverifikasi (2026-08) di WSL: anomalib 2.5.0, numpy 2.4.6,
# torch 2.13.0+cpu, ultralytics 8.4.117, openvino 2026.2.1, nncf 3.2.0,
# lightning 2.6.5, PySide6 6.6.3.1.

# ===== GUI =====
PySide6>=6.6.0,<6.7

# ===== Computer Vision =====
opencv-python-headless>=4.9.0,<5.0
numpy>=1.24,<3.0
timm>=1.0,<1.1
huggingface_hub>=0.24,<2.0

# ===== Anomaly Detection (Anomalib) =====
anomalib[core]>=1.1,<3.0
scikit-learn>=1.3,<2.0
requests>=2.28,<3.0
python-dotenv
pandas>=2.0,<4.0
matplotlib>=3.7,<4.0
# lightning: diimport langsung oleh core/training.py (EarlyStopping)
lightning>=2.2,<3.0

# CATATAN: torch & torchvision SUDAH diinstall via:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
# Tidak perlu di sini agar tidak konflik hash.

# ===== YOLO (training klasifikasi OK/NG — sesi 2026-08) =====
# YOLO11 butuh ultralytics >= 8.3; 8.4.117 teruji (patience, export OpenVINO)
ultralytics>=8.3,<9.0

# ===== OpenVINO Inference & INT8 Quantization =====
openvino>=2024.0
onnx>=1.16.0
onnxscript
# nncf: dipakai core/training.py (export INT8) — kalau tidak ada, INT8 di-skip
nncf>=3.0,<4.0

# ===== Serial / PLC =====
pyserial>=3.5,<4.0
pymodbus>=3.6,<4.0

# ===== Keamanan / API internal =====
# cryptography: enkripsi Fernet secret_store (password PG)
cryptography>=42.0
# werkzeug: diimport langsung oleh api/flask_app.py (make_server)
werkzeug>=3.0,<4.0

# ===== Optional Internal API =====
flask>=3.0,<4.0

# ===== Testing =====
pytest>=8.0,<10.0
pytest-qt>=4.2,<5.0
pytest-mock>=3.12,<5.0

# ===== PostgreSQL =====
psycopg2-binary>=2.9,<3.0

# ===== Monitoring =====
psutil>=5.9,<8.0
